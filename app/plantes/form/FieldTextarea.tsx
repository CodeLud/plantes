/* import { Textarea } from "@/components/ui/textarea";
import { FormFieldComponent } from "./2-FieldComponent";
import { FormFieldComponentProps } from "./typesInterfaceCopy"; // Importez l'interface commune

export const FormTextareaComponent = ({
  name,
  label,
  placeholder,
  description,
}: FormFieldComponentProps) => {
  return (
    <FormFieldComponent
      name={name}
      label={label}
      placeholder={placeholder}
      description={description}
      as={Textarea} // Utilisez Textarea comme composant personnalisé
      asProps={{ className: "resize-none" }} // Propriétés supplémentaires pour Textarea
    />
  );
};
 */
